<!DOCTYPE html>
<html>
  <head>
    <title>Photo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'index', 'data-turbolinks-track': 'reload' %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body>
  <div class="d-flex justify-content-between mb-3">
    <div>
        <h1><%= link_to('MyInstagramCopyCat', root_path, class: "navbar-brand") %></h1>
    </div>
    <div></div>
    <div>
        <% if user_signed_in? %>
          <div class="dropdown text-right-end">
            <button type="button" class="btn btn-primary dropdown-toggle rounded-pill" data-bs-toggle="dropdown" style="width:40px; height:40px;">
              <%= current_user.name[0] %>
            </button>
          
            <ul class="dropdown-menu dropdown-menu-end">
              <li><span class="dropdown-item-text"><%= current_user.name %></span></li>
              <li><a class="dropdown-item" href="#">add image</a></li>
              <li><hr class="dropdown-divider"></hr></li>

              <li><%= link_to('Edit profile', edit_user_registration_path, class: "dropdown-item") %></li>
              <li><%= button_to('Logout', destroy_user_session_path, method: :delete, class: "dropdown-item") %></li>

            </ul>
          </div>
        <% else %>
          <%= link_to('Login', new_user_session_path)  %>
          <%= link_to('Sign Up', new_user_registration_path)  %>  
        <% end %>`
      
      </div>
    </div>
    <%= yield %>

  </body>
</html>
